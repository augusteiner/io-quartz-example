plugins {
    id 'java'
    id 'application'
}

group 'io.quartz.example'
version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.quartz-scheduler:quartz:2.3.2'

    testCompile group: 'junit', name: 'junit', version: '4.12'
}
